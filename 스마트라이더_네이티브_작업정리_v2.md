# 스마트라이더 네이티브 앱 개발 정리 (v2)

> ⚠️ **오실장 = 클로드(AI 어시스턴트)를 부르는 호칭.** 웜대표님을 부르는 말이 아님. (유래: 클로드 → 클오드 → 오실장)
> 마지막 업데이트: 2026-09-01
> 다음 세션에서 이 문서를 먼저 읽고 작업을 이어갈 것

---

## 1. 프로젝트 개요

### 무엇을 만들고 있나
기존 **웹앱(PWA) 스마트라이더**를 **React Native 네이티브 앱**으로 전환 중.
- 웹앱의 한계: 백그라운드(다른 앱 사용 중)에서 토스트 알림이 안 뜸
- 네이티브 전환 목적: 배민/쿠팡 등 다른 앱을 쓰는 중에도 등록된 건물 근처에 가면 알림을 띄우는 것

### 앱의 정체성
- 라이더가 배달하며 수집한 건물 출입정보(비밀번호, 출입방법 등)를 저장/조회하는 앱
- 등록된 건물 근처 진입 시 알림으로 출입정보를 띄워줌
- 향후 전국 라이더 대상으로 확장 예정 (지역별 데이터 분리 기능 포함)

### 웹앱을 아직 병행 사용 중인 이유
- 네이티브 앱에서 한글 자판(한영전환)이 폰 기본 버튼으로는 잘 안 붙는 경우가 있어, 앱 내부에 강제 한영전환 버튼을 별도로 만들어 대응 중
- 근본 수정보다 이 우회 버튼을 유지하는 것으로 결론 (RN TextInput IME 이슈, 완전 해결 후순위)

### ⚠️ 배포 전 필수 (절대 잊지 말 것)
Play Store 배포 전 **반드시 데이터 분리** 필요:
- **개인 수집 메모** (출입 비밀번호 등) → 사용자 본인만 접근 가능하게 처리
- **플랫폼 제공 정보** (대형 상가단지 엘베정보, 상점위치 등) → Firebase (공개 가능한 것만, 관리자만 입력)
- 개인정보 포함 상태로 배포 절대 금지

---

## 2. 환경 / 경로 정보

| 항목 | 값 |
|------|-----|
| 네이티브 프로젝트 경로 | `D:\app\SmartRiderNative3` |
| 웹앱 프로젝트 경로 | `D:\app\building-access-project` |
| 네이티브 GitHub | https://github.com/richcompany12/SmartRiderNative.git (브랜치: **master**) |
| 웹앱 GitHub | https://github.com/richcompany12/building-access-project.git (브랜치: **main**) |
| 패키지명 | `com.richcompany.smartridernative3` (전부 소문자!) |
| Expo SDK | 54 |
| 카카오 JavaScript 키 | `7d65ade73c1b3e7d64687306911f7ce7` |
| Firebase 프로젝트 | building-access-project |
| 건물 데이터 수 | 약 813개 |
| 테스트 기기 | 갤럭시 S26 Ultra (SM-S948N) |

### 관리자 UID (constants.js의 ADMIN_UIDS)
```
kSeIdvGyZkPceeKFBhbusVSIJOe2   (richcompany@gmail.com 이메일 계정)
jWzcX90fauhcefeAkUWSqoewn7V2   (원래 구글 로그인 계정)
MsrzbIrzUfeAecSwXt6pX88d9mY2
fkvDpI9MBUeWolC2EqgjcdOXKxa2
4tkRkvbmyxThhgpmirppLJPvde23
```
> 네이티브 앱은 현재 **이메일 로그인만** 지원. 구글 로그인 미구현.
> 관리자 권한 테스트는 richcompany@gmail.com + 비밀번호로 이메일 회원가입하여 사용.

---

## 3. 완성된 기능 (현재 작동 중)

- ✅ 이메일 로그인 / 회원가입 (Firebase Auth)
- ✅ 건물 목록 조회 (페이지네이션, 8개씩)
- ✅ 건물 검색 (초성/텍스트/숫자 검색, SK 단축키)
- ✅ 건물 등록 (일반 건물 / 강력 알림 탭, 단축키 버튼)
- ✅ 건물 상세 (조회, 관리자만 수정/삭제)
- ✅ 위치 수정 (수정 모드 → 지도에서 핀 찍기)
- ✅ 카카오맵 (WebView 기반, release 빌드에서도 작동)
- ✅ 지도 길게 누르기 → 등록 (인근 건물 참조 복사)
- ✅ 백그라운드 위치 감지 + 시스템 알림 (다른 앱 위에서도 알림 옴)
- ✅ 토스트 카드 (앱 포그라운드일 때, 최대 3개/15초 자동삭제/탭 고정/스와이프 닫기)
- ✅ release 빌드 독립 실행 (메트로/데스크탑 불필요)

---

## 4. 파일 구조

```
D:\app\SmartRiderNative3\
├── App.js                          # 네비게이션 + AuthProvider + ProximityNotifier
├── firebase.js                     # Firebase 초기화 (웹앱에서 가져옴, getAnalytics 제거)
├── firebaseDB.js                   # DB 함수 (웹앱 그대로)
├── constants.js                    # ADMIN_UIDS 등 (웹앱에서 가져옴)
├── AuthContext.js                  # 로그인 상태 관리
├── app.json                        # 권한, expo-location 플러그인 설정
└── screens\
    ├── HomeScreen.js               # 홈 (목록 + 새로고침 버튼)
    ├── SearchScreen.js             # 검색
    ├── RegisterScreen.js           # 등록
    ├── DetailScreen.js             # 상세 (위치수정 버튼 포함)
    ├── MapScreen.js                # 카카오맵 (WebView)
    ├── LoginScreen.js              # 로그인
    ├── LocationPickerScreen.js     # 위치 선택 전용 지도
    ├── ProximityNotifier.js        # 위치감지 + 알림 + 토스트 관리 (핵심)
    └── ProximityToastOverlay.js    # 토스트 카드 UI 컴포넌트
```

---

## 5. 빌드 / 실행 방법

### 개발 모드 (코드 수정하며 테스트, 데스크탑 옆에서)
```powershell
cd D:\app\SmartRiderNative3
npx expo run:android
```
- 메트로 서버 연결됨, 코드 수정 시 즉시 반영
- 가상폰(에뮬레이터) 켜져있으면 그쪽으로 갈 수 있으니 주의 → 가상폰 닫고 진짜 폰만 연결

### release 빌드 (독립 실행, 필드 테스트용)
```powershell
cd D:\app\SmartRiderNative3\android
.\gradlew.bat assembleRelease -x lint -x lintVitalAnalyzeRelease
```
설치:
```powershell
adb install -r D:\app\SmartRiderNative3\android\app\build\outputs\apk\release\app-release.apk
```
- `-x lint` 빼면 메모리 부족(OutOfMemory)으로 빌드 실패함 → 꼭 붙일 것
- 설치 후 메트로/데스크탑/와이파이 없이 독립 실행됨 (데이터만 있으면 Firebase 통신 OK)

### 무선 디버깅 연결 (밖에 나갔다 오면 IP 바뀜)
```powershell
adb pair 192.168.0.XX:포트     # 페어링 (코드 입력)
adb connect 192.168.0.XX:메인포트   # 연결
adb devices                     # device 떠야 정상
```

---

## 6. 빌드하며 겪은 문제 & 해결법 (다음에 또 만날 수 있음)

### 6-1. Expo Go SDK 버전 불일치
- SDK 56은 Expo Go 미지원 → SDK 54로 다운그레이드하여 해결

### 6-2. Kotlin 버전 충돌
- `android/build.gradle`에 `kotlinVersion = "2.2.0"` 명시 필요
- (react-native-async-storage가 2.2.0 요구)

### 6-3. 패키지명 불일치 (autolinking.json 캐시)
- 증상: `package com.smartridernative3 does not exist`
- 원인: `android/build`, `android/app/build` 폴더의 옛 캐시
- 해결: 두 build 폴더 삭제 후 재빌드

### 6-4. SoLoader 에러
- `MainApplication.kt`에서 `loadReactNative` import 제거 관련 이슈
- newArchEnabled를 false로 설정

### 6-5. expo-notifications가 Expo Go에서 안 됨
- SDK 53부터 Expo Go에서 푸시 알림 제거됨
- → 개발 빌드/release 빌드에서는 정상 작동 (Expo Go만 안 됨)

### 6-6. 백그라운드 위치 "항상 허용" 옵션이 안 보임
- 원인: app.json에 백그라운드 위치 권한 미선언
- 해결: app.json android.permissions에 `ACCESS_BACKGROUND_LOCATION` 등 추가 + expo-location 플러그인 설정

### 6-7. ⭐ release 빌드에서 카카오맵 안 뜸 (가장 오래 걸린 문제)
- **원인: release 빌드의 cleartext(http) 통신 차단**
- 카카오맵이 내부 리소스를 http로 불러오는데 release에서 막힘
- **해결: AndroidManifest.xml의 `<application>` 태그에 `android:usesCleartextTraffic="true"` 직접 추가**
- ⚠️ **주의: `npx expo prebuild` 다시 하면 이 수정이 날아감!** → expo-build-properties 플러그인으로 영구 적용 필요 (미해결)

### 6-8. react-native-toast-message 패키지 에러
- BaseToast.styles 모듈 못 찾음 → 패키지 제거하고 직접 토스트 컴포넌트 구현 (ProximityToastOverlay.js)

---

## 7. 신규 설계 결정 사항 (2026-09-01 회의 정리)

### 7-1. 데이터 구조 3분리
| 종류 | 저장 위치 | 접근 권한 | 비고 |
|------|-----------|-----------|------|
| 개인 메모 (출입 비밀번호 등) | Firebase `users/{uid}/personalNotes/{buildingId}` | 본인만 (`request.auth.uid == uid` 보안규칙) | 로컬 대신 본인전용 클라우드로 결정 → 폰 변경 시 로그인만 하면 자동 복원 |
| 공공데이터 (엘베정보, 상가배치도 등) | Firebase 공용 컬렉션 | 관리자(ADMIN_UIDS)만 입력/수정 | 지역별 등록권한 분리는 **하지 않기로 결정** — 기존 관리자 구조 그대로 유지 |
| 지역 메타데이터 | Firebase `regions` 컬렉션 | 관리자 | 아래 7-2 참고 |

### 7-2. 지역별 데이터 선택 기능
- **계층형 선택**: 1단계 도/시 드롭다운 → 2단계 시/구 체크박스 (체크박스 개수 무한정 늘어나는 것 방지)
- Firestore 구조 예시:
  ```
  regions/{provinceId}: { name: "경기도", noticeMessage: "현재 등록된 지역입니다. 추후 제보 등을 통해 업데이트 예정입니다." }
  regions/{provinceId}/districts/{districtId}: { name: "동탄", active: true }
  ```
- 지역 추가 시 **앱 업데이트(구글 심사) 없이** Firestore 데이터만 추가하면 됨
- 도 선택 시 그 아래에 안내 문구 자동 노출 (문구도 Firestore 필드로 관리 → 코드 수정 없이 문구 변경 가능)
- 사용자가 선택한 지역: `users/{uid}.selectedDistricts` 배열에 저장, 해당 지역 공공데이터만 노출
- 초기 지역: 경기도(동탄/병점/오산/수원)

### 7-3. 플로팅 버튼 + 화면 스캔 워크플로우
- 플로팅 버튼: [[fishline]]의 `FloatingButtonService.kt` / `FloatingButtonModule.kt` / `FloatingButtonPackage.kt` 재사용 이식
- 배민/쿠팡 화면 위에서 플로팅 버튼으로 스마트라이더 기능 즉시 호출
- 화면 스캔: AccessibilityService로 화면 텍스트 읽기 → Gemini API로 건물명/주소 추출
- 스캔한 데이터는 **바로 등록하지 않고 "작업대기함"에 임시 저장**
- 근무 종료 후 사무실에서 작업대기함을 보면서 직접 등록하거나 삭제 (기존 데이터와의 **자동 비교 UI는 만들지 않기로 결정** — 오실장이 직접 판단)
- 오프라인 상태(배달 중 와이파이 안 터지는 곳)에서도 스캔 데이터는 로컬에 쌓였다가 나중에 동기화되는 구조 필요

### 7-4. 중복 데이터 판별
- **좌표 반경 체크**: 등록 화면에서 저장 전 반경 30m 이내(기존 ProximityNotifier의 RADIUS 값과 통일) 기존 건물이 있는지 자동 조회
- 반경 내 기존 건물 있으면 팝업으로 알려주고, 최종 판단(같은 곳/다른 곳)은 사람이 함 — 자동 병합은 하지 않기로 함 (오판 리스크 방지)
- **별칭(aliases) 필드**: 같은 건물을 여러 이름으로 등록해온 문제 해결용
  ```
  buildings/{id}: { name: "대표이름", aliases: ["다른표현1", "다른표현2"], lat, lng, ... }
  ```
  같은 건물로 확인되면 새 이름은 대표이름을 바꾸지 않고 aliases에 추가 → 검색 시 어떤 표현으로 찾아도 매칭됨

### 7-5. 개인데이터 백업/복원 유료화
- 개인 메모는 항상 Firebase(`users/{uid}/personalNotes`)에 자동 저장 (7-1 참고) — 저장 자체는 무료, 별도 백업 신청 불필요
- **복원 기능만 유료(₩10,000)로 잠금**
- 최초 검토했던 "전화로 입금 확인 후 수동 계정 활성화" 방식은 **확장성/세금/신뢰 문제로 폐기**
- **구글 플레이 인앱결제(Play Billing)로 자동화**하기로 결정: 결제 즉시 앱이 영수증 확인 후 자동으로 복원 기능 활성화, 오실장 수동 개입 없음
- Firebase 저장은 기본 암호화(at-rest) + `uid` 기반 보안규칙으로 이미 비공개 처리됨, 별도 암호화 로직 불필요

### 7-6. 광고 계획
- 앱 실행 시 전면광고 1회 + 하단 상시 배너광고
- ⚠️ **AdMob 연동 필요**: `react-native-google-mobile-ads`가 RN 0.81에서 `currentActivity` API 제거 문제로 막혀있음 — [[fishline]]과 동일한 이슈 예상, 우회 방법 사전 확인 필요

---

## 8. 다음 할 일 (우선순위 순)

1. **데이터 구조 재설계 착수** — 개인데이터(uid별 비공개 컬렉션) / 공공데이터(관리자 권한) / 지역태그 스키마 확정
2. **시스템 오버레이 토스트** — 다른 앱(쿠팡/배민) 위에서도 토스트 카드 노출 (SYSTEM_ALERT_WINDOW + WindowManager, 네이티브 Kotlin)
3. **플로팅 버튼 이식** — 피쉬라인 Kotlin 파일 재사용
4. **화면 스캔 → 작업대기함** — AccessibilityService + Gemini API, 비교 UI 없이 리스트만 쌓기
5. **지역 선택 UI** — 도→시/구 계층형 드롭다운+체크박스, Firestore regions 컬렉션
6. **좌표 반경 중복 체크 + aliases 필드** — 등록 화면에 반경 30m 조회 로직 추가
7. **AdMob 우회 방법 확인** — 피쉬라인과 공통 이슈, 먼저 해결하면 두 앱 다 적용 가능
8. **인앱결제 연동** — 복원 기능 잠금해제용 Play Billing 상품 등록
9. **전면광고 + 배너광고 적용** — AdMob 문제 해결 후 진행
10. **cleartext 영구 적용** — expo-build-properties로 prebuild해도 안 날아가게 (기존 미해결 과제)

---

## 9. 기술 메모 (기존)

- 건물 목록은 앱 시작 시 1회만 로드 후 메모리 재사용 (Firebase 폭탄 방지) — 단, 앱 켜둔 채로는 갱신 안 됨
- 감지 반경 RADIUS = 30m, 쿨다운 COOLDOWN = 5분(300000ms)
- 토스트 자동삭제 = 15초(15000ms), 최대 3개
- 카카오맵은 WebView + postMessage로 RN ↔ 지도 통신
- 위치 권한은 반드시 "항상 허용"이어야 백그라운드 작동
- 토스트는 App.js에서 ProximityNotifier를 Stack.Navigator **뒤에** 둬야 화면 위에 보임 (안 그러면 묻힘)
